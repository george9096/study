<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study.auth.AuthMapper">
    <insert id="insertUser">
        insert into users (email, password_hash, name)
        values (#{email}, #{passwordHash}, #{name})
    </insert>
    <select id="findByEmail" resultType="com.example.study.auth.UserDto">
        select
            id,
            email,
            password_hash,
            name,
            created_at
        from users
        where email = #{email}
    </select>
    <select id="findById" resultType="com.example.study.auth.UserDto">
        select
            id,
            email,
            password_hash,
            name,
            created_at
        from users
        where id = #{id}
    </select>
</mapper>
